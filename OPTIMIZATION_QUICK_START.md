# 🚀 優化版本快速啟動指南

> **重要提示**：您正在使用經過優化的 EduVibe 3D v2.0（Optimization 分支）

---

## ⚡ 5分鐘快速啟動

### 1️⃣ 配置 API Key

創建 `.env` 文件在項目根目錄：

```bash
# Windows PowerShell
Copy-Item .env.example .env

# 或者手動創建 .env 文件，內容如下：
```

```env
MINIMAX_API_KEY=your-api-key-here
MINIMAX_BASE_URL=https://vip.dmxapi.com/v1
MINIMAX_MODEL=gpt-4o
PORT=3000
```

**📝 注意**：請將 `your-api-key-here` 替換為您的真實 MiniMax API Key

### 2️⃣ 安裝依賴

```bash
npm install
```

### 3️⃣ 啟動項目

```bash
npm run dev
```

### 4️⃣ 訪問應用

打開瀏覽器訪問：**http://localhost:5173**

---

## ✨ 新功能體驗指南

### 🤖 自然語言控制台

1. 在 3D 場景頁面，點擊右上角的 **"🤖 顯示自然語言控制台"** 按鈕
2. 在控制台中輸入指令，例如：
   - "開始演示"
   - "設置數組為 5,2,8,1,9"
   - "切換到數據視角"
   - "下一步"

### 👁️ 多視角切換

在右側控制面板找到 **"👁️ 視角切換"** 區域：
- **🌍 上帝視角**：全局俯視/側視
- **🔍 數據視角**：第一人稱，進入數據結構內部

### 💻 代碼面板

在 3D 場景左下角，點擊 **"💻 顯示代碼"** 按鈕：
- 查看當前算法的完整代碼
- 觀察代碼行高亮（與動畫步驟同步）

### 📚 調用棧可視化（遞歸算法）

對於遞歸算法（如漢諾塔、樹遍歷），右側面板會自動顯示：
- 當前調用棧
- 函數名稱和參數
- 棧頂高亮顯示

### ⚠️ 錯誤情境體驗（漢諾塔）

生成"漢諾塔"算法後：
1. 嘗試手動操作將大盤子放到小盤子上
2. 觀察紅色警報特效：
   - 震動動畫
   - 紅色閃爍
   - 錯誤提示彈窗

---

## 🎨 美學升級對比

### 優化前
- ❌ 使用簡單的 `MeshBasicMaterial`
- ❌ 單一環境光 + 方向光
- ❌ 純色或黑色背景
- ❌ 簡單的文字標籤

### 優化後
- ✅ PBR 物理渲染材質（`MeshStandardMaterial`）
- ✅ 五光源專業布光（環境光 + 主光 + 輪廓光 + 補光 + 半球光）
- ✅ 科技感漸變背景
- ✅ 瑞士國際主義風格排版
- ✅ 磨砂玻璃效果（Glassmorphism）
- ✅ 軟陰影（Soft Shadows）

---

## 🧪 測試建議

### 推薦測試的概念

1. **二分查找**
   - 測試：代碼面板 + 指針動畫
   - 體驗：自然語言輸入 "設置數組為 1,3,5,7,9,11"

2. **快速排序**
   - 測試：調用棧可視化
   - 體驗：觀察遞歸調用過程

3. **二叉樹遍歷**
   - 測試：動態連線 + 數據流動畫
   - 體驗：切換到數據視角

4. **漢諾塔**
   - 測試：錯誤檢測 + 紅色警報
   - 體驗：嘗試非法移動

5. **深度優先搜索**
   - 測試：完整的教育體驗
   - 體驗：多視角 + 代碼映射 + 堆棧可視化

---

## 🐛 常見問題排查

### Q1: 提示 "未設置 MINIMAX_API_KEY"

**解決方案**：
1. 確認 `.env` 文件在項目根目錄
2. 確認 `MINIMAX_API_KEY` 已設置且不為空
3. 重啟後端服務器

### Q2: 自然語言控制台不響應

**解決方案**：
1. 檢查瀏覽器控制台是否有錯誤
2. 確認已生成 3D 場景（不是在首頁）
3. 刷新頁面重試

### Q3: 3D 場景無法顯示

**解決方案**：
1. 檢查 `backend/public/libs/` 是否包含 Three.js 庫文件
2. 檢查後端是否正常運行（http://localhost:3000）
3. 清除瀏覽器緩存

### Q4: 代碼面板是空的

**解決方案**：
這是正常的！代碼內容由 AI 動態生成，會在 defineSteps() 函數中調用 setCodeContent() 設置。

---

## 📁 重要文件位置

```
Education_3D/
├── OPTIMIZATION_SUMMARY.md       # 詳細優化報告（必讀！）
├── OPTIMIZATION_QUICK_START.md   # 本文檔
├── .env                           # 環境變量（需創建）
├── .env.example                   # 環境變量示例
│
├── backend/
│   └── src/
│       └── services/
│           └── promptEngine.ts    # 核心優化文件（1500+ 行）
│
└── frontend/
    └── src/
        └── components/
            ├── NaturalLanguageConsole.tsx  # 自然語言控制台（新增）
            └── Sandbox3D.tsx               # 3D 沙箱（已優化）
```

---

## 🎯 優化完成清單

- [x] ✅ 模塊一：MiniMax M2.1 美學重構
- [x] ✅ 模塊二：幾何修正與 Bug 根除
- [x] ✅ 模塊三：自然語言與智能交互
- [x] ✅ 模塊四：深度教育功能增強
- [x] ✅ 模塊五：Debug 和調試

**所有優化已完成！🎉**

---

## 📞 獲取幫助

- **詳細優化報告**：閱讀 `OPTIMIZATION_SUMMARY.md`
- **項目文檔**：閱讀 `README.md`
- **啟動指南**：閱讀 `GETTING_STARTED.md`

---

<div align="center">

**🎓 EduVibe 3D v2.0 - 讓學習更生動** ✨

優化分支：Optimization  
優化日期：2026-01-23

</div>
